import { getUserBitcoin,UserDollars } from "./coindesk-service";
import { Amount } from "./index";
export function renderResults(UserDollars) {
    var UserDollars;
    $get.(getUserBitcoin);
    $("#conversion-results").html(`${Amount}`);
}
// how to render resultss in 